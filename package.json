{
  "name": "tubegenius",
  "version": "1.0.0",
  "description": "Full-stack Django & Next.js platform with comprehensive quality checks",
  "private": true,
  "scripts": {
    "check": "./scripts/check-all.sh",
    "check:frontend": "cd frontend && pnpm lint && pnpm prettier:check && pnpm type-check",
    "check:backend": "cd backend && uv run ruff check . && uv run ruff format --check .",
    "format": "cd frontend && pnpm prettier:fix && pnpm lint:fix"
  },
  "engines": {
    "node": ">=20.18.0",
    "pnpm": ">=9.14.0"
  },
  "workspaces": [
    "frontend"
  ]
}
