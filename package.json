{
  "name": "tubegenius",
  "version": "1.0.0",
  "description": "Full-stack Django & Next.js platform with comprehensive quality checks",
  "private": true,
  "scripts": {
    "website:start": "pnpm --filter=website run start",
    "web-app:start": "pnpm --filter=web-app run start",
    "admin-panel:start": "pnpm --filter=admin-panel run start",
    "website:build": "pnpm --filter=website run build",
    "web-app:build": "pnpm --filter=web-app run build",
    "admin-panel:build": "pnpm --filter=admin-panel run build",
    "website": "pnpm --filter=website run dev",
    "web-app": "pnpm run --filter=web-app dev",
    "admin-panel": "pnpm run --filter=admin-panel dev",
    "check": "./scripts/check-all.sh",
    "check:website": "cd website && pnpm lint && pnpm prettier:check && pnpm type-check",
    "check:web-app": "cd web-app && pnpm lint && pnpm prettier:check && pnpm type-check",
    "check:backend": "cd backend && uv run ruff check . && uv run ruff format --check .",
    "format:backend": "cd backend && uv run ruff check . && uv run ruff format --check .",
    "format": "cd website && pnpm prettier:fix && pnpm lint:fix ",
    "format:admin-panel": "cd admin-panel && pnpm prettier:fix && pnpm lint:fix"
  },
  "engines": {
    "node": ">=20.18.0",
    "pnpm": ">=9.14.0"
  },
  "workspaces": [
    "web-app",
    "website",
    "admin-panel"
  ],
  "dependencies": {
    "lucide-react": "^0.540.0",
    "next": "15.3.6"
  }
}
